how to share stuff

import useState

function App() {
    const {name, function} = useStaet("")
    return (
        <ComponentA setSharedValue={functoin}>
        <ComponentB sharedValue={name}>
    )
}

funcrtion set ({ function }) {
    function(variable to share)
    return ()
}

function getShared ({name}) {
    return (
        <p>{name} </p>
    )
}

stringify -> parse -> . bs


type="datetime-local"
